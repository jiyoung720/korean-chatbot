# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# 환경변수
.env

# 모델 가중치 / 토크나이저 / 큰 바이너리 (모든 하위 경로 포함)
artifacts/**/*.pt
artifacts/**/*.model
artifacts/**/*.vocab
artifacts/*.pt
artifacts/*.model
artifacts/*.vocab

# 데이터 원본/가공본 (모든 하위 경로 포함)
data/raw/
data/processed/

# OS
.DS_Store

# IDE
.vscode/
artifacts/rag/
# 추가 데이터/캐시 (RAG 코퍼스, 임베딩 캐시)
data/*.txt
artifacts/rag/
